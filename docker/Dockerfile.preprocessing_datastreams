FROM python:3.12-slim

WORKDIR /app

COPY docker/requirements.preprocessing_datastreams.txt .
RUN pip install --no-cache-dir -r requirements.preprocessing_datastreams.txt

COPY src/ /app/src/
COPY params.yaml .
COPY setup.py .
COPY .env /app/.env

#CMD ["python", "-m", "src.data.make_dataset"]

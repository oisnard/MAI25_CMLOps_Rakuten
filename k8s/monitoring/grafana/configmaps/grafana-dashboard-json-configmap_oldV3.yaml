apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-json
  namespace: monitoring
data:
  rakuten_dashboard.json: |
    {
      "id": null,
      "uid": "rakuten-api-dashboard",
      "title": "Rakuten API Dashboard",
      "tags": ["api", "rakuten-api", "prometheus"],
      "timezone": "browser",
      "schemaVersion": 38,
      "version": 2,
      "refresh": "5s",
      "panels": [
        {
          "type": "graph",
          "title": "Total HTTP Requests",
          "targets": [
            {
              "expr": "sum by (method, status) (http_requests_total{job=\"rakuten-api\"})",
              "legendFormat": "{{method}} {{status}}",
              "refId": "A"
            }
          ],
          "gridPos": {"h": 8, "w": 8, "x": 0, "y": 0}
        },
        {
          "type": "graph",
          "title": "Request Duration (seconds)",
          "targets": [
            {
              "expr": "rate(http_request_duration_seconds_bucket{job=\"rakuten-api\"}[1m])",
              "legendFormat": "{{le}}",
              "refId": "A"
            }
          ],
          "gridPos": {"h": 8, "w": 8, "x": 8, "y": 0}
        },
        {
          "type": "graph",
          "title": "CPU Time (seconds)",
          "targets": [
            {
              "expr": "rate(process_cpu_seconds_total{job=\"rakuten-api\"}[1m])",
              "legendFormat": "CPU",
              "refId": "A"
            }
          ],
          "gridPos": {"h": 8, "w": 8, "x": 0, "y": 8}
        },
        {
          "type": "stat",
          "title": "Memory Usage (Resident)",
          "targets": [
            {
              "expr": "process_resident_memory_bytes{job=\"rakuten-api\"}",
              "legendFormat": "Memory",
              "refId": "A"
            }
          ],
          "gridPos": {"h": 8, "w": 4, "x": 8, "y": 8}
        },
        {
          "type": "graph",
          "title": "GC Collections",
          "targets": [
            {
              "expr": "rate(python_gc_collections_total{job=\"rakuten-api\"}[1m])",
              "legendFormat": "Gen {{generation}}",
              "refId": "A"
            }
          ],
          "gridPos": {"h": 8, "w": 4, "x": 12, "y": 8}
        },
        {
          "type": "graph",
          "title": "HTTP Requests per Second",
          "targets": [
            {
              "expr": "rate(http_requests_total{job=\"rakuten-api\"}[1m])",
              "legendFormat": "{{method}} {{status}}",
              "refId": "A"
            }
          ],
          "gridPos": {"h": 8, "w": 8, "x": 0, "y": 16}
        }
      ],
      "templating": {"list": []},
      "annotations": {"list": []},
      "time": {"from": "now-15m", "to": "now"}
    }

  rakuten_k8s_dashboard.json: |
    {
      "id": null,
      "uid": null,
      "title": "Rakuten - K8s Monitoring",
      "tags": ["kubernetes", "rakuten", "monitoring"],
      "timezone": "browser",
      "schemaVersion": 16,
      "version": 0,
      "refresh": "5s",
      "panels": [
        {
          "type": "stat",
          "title": "Number of rakuten-api Pods",
          "id": 1,
          "datasource": "Prometheus",
          "targets": [
            {
              "expr": "count(kube_pod_info{pod=~\"rakuten-api.*\"})",
              "legendFormat": "Pods",
              "refId": "A"
            }
          ],
          "gridPos": {"h": 6, "w": 8, "x": 0, "y": 0}
        },
        {
          "type": "graph",
          "title": "CPU Usage (millicores)",
          "id": 2,
          "datasource": "Prometheus",
          "targets": [
            {
              "expr": "sum(rate(container_cpu_usage_seconds_total{pod=~\"rakuten-api.*\"}[1m])) * 1000",
              "legendFormat": "{{pod}}",
              "refId": "A"
            }
          ],
          "gridPos": {"h": 8, "w": 12, "x": 0, "y": 6}
        },
        {
          "type": "graph",
          "title": "Memory Usage (MiB)",
          "id": 3,
          "datasource": "Prometheus",
          "targets": [
            {
              "expr": "sum(container_memory_usage_bytes{pod=~\"rakuten-api.*\"}) / 1024 / 1024",
              "legendFormat": "{{pod}}",
              "refId": "A"
            }
          ],
          "gridPos": {"h": 8, "w": 12, "x": 0, "y": 14}
        }
      ]
    }
